{
  "name": "paging-routing",
  "github": "https://github.com/gbroberts/paging-routing",
  "version": "1.0.0",
  "description": "An app to manage your todos",
  "directories": {
    "doc": "docs"
  },
  "default": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run static && npx parcel src/index.html",
    "static": "npx gulp",
    "dev": "",
    "build": "npm run static && npx parcel build src/index.html",
    "clean:dev": "rm -rf .parcel-cache dist",
    "clean:git": "rm -rf .parcel-cache dist  node_modules"
  },
  "author": "Graham Roberts",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "gulp": "^4.0.2",
    "http-server": "^13.0.2",
    "parcel": "^2.0.0-rc.0",
    "sass": "^1.40.1"
  },
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
